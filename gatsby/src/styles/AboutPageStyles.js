import styled from 'styled-components';

const AboutPageStyles = styled.div`
    .about-info{
      text-align: center;
      display: grid;
      place-items: center;
    }
`;

export default AboutPageStyles;