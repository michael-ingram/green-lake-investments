import styled from 'styled-components';

const AboutPageStyles = styled.div`
    .about-info{
      text-align: center;
      display: grid;
      place-items: center;
      margin: 1rem;
      top: -50%;
      right: -50%;
    }
`;

export default AboutPageStyles;