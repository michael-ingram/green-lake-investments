import styled from "styled-components";

const FooterStyles = styled.footer`
  text-align: center;
  position: absolute;
  left: 0;
  bottom: 0;
  height: 50px;
  width: 100%;
    p {
      font-size: 1rem;
      left: 0;
      color: var(--offwhite);
    }
`;

export default FooterStyles;